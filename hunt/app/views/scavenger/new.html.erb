<h1> Create a new scavenger hunt</h1>

<%= form_for(@scavenger, :method => :post, :url => {:action => :create}) do |f| %>
	<% if @scavenger.errors.any? %>
	    <div id="error_explanation">
	      <h2><%= pluralize(@scavenger.errors.count, "error") %> prohibited this
	      Scavenger from being saved:</h2>

	      <ul>
	      <% @scavenger.errors.full_messages.each do |msg| %>
	        <li><%= msg %></li>
	      <% end %>
	      </ul>
    	</div>
	<% end %>
	  <p><%= f.label :hunt_name %></p>
      <p><%= f.text_field :hunt_name %></p>

      <p><%= f.label :description %></p>
      <p><%= f.text_field :description %></p>

      <p><%= f.label :location %></p>
      <p><%= f.text_field :location %></p>
	<p><%= submit_tag("Submit") %></p>
<% end %>