<h1> Create a new task</h1>

<%= form_for(@task, :method => :post, :url => {:controller => :scavenger, :action => :add_task}) do |f| %>
	<% if @task.errors.any? %>
	    <div id="error_explanation">
	      <h2><%= pluralize(@task.errors.count, "error") %> prohibited this
	      Task from being saved:</h2>

	      <ul>
	      <% @task.errors.full_messages.each do |msg| %>
	        <li><%= msg %></li>
	      <% end %>
	      </ul>
    	</div>
	<% end %>
	  <p><%= f.label :name %></p>
      <p><%= f.text_field :name %></p>

      <p><%= f.label :point %></p>
      <p><%= f.text_field :point %></p>
	<p><%= submit_tag("Submit") %></p>
<% end %>